<h1>Create a new review:</h1>
<br>
<%= simple_form_for ([@restaurant, @review]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.button :submit, class: "btn-primary" %>
<% end %>
<%= link_to "Back to the list", restaurant_path(@restaurant)  %>
